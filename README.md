# shiny_textarea_input
This repository contains a basic shiny application that allows the user to paste text into a textarea box and parses that text into a dataframe. The main idea is to use this to allow on-the-fly table lookups/merges.


